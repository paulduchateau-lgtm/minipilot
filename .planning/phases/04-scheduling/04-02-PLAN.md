---
phase: 04-scheduling
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - minipilot/app/src/lib/api.js
  - minipilot/app/src/components/SchedulePage.jsx
  - minipilot/app/src/components/ScheduleListPage.jsx
  - minipilot/app/src/components/WorkspaceShell.jsx
  - minipilot/app/src/components/Sidebar.jsx
autonomous: false
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05]

must_haves:
  truths:
    - "User can create a new schedule by navigating to the scheduling wizard from sidebar"
    - "User can pick frequency (once/daily/weekly/monthly) and configure time, day, end date"
    - "User can select an existing report or type a new description as template"
    - "User can pick a data source file from workspace uploads"
    - "User can view a list of active schedules with their status and edition count"
    - "User can pause, resume, delete, and manually trigger a schedule"
  artifacts:
    - path: "minipilot/app/src/components/SchedulePage.jsx"
      provides: "4-step scheduling wizard (template, schedule config, data source, confirm)"
      min_lines: 150
    - path: "minipilot/app/src/components/ScheduleListPage.jsx"
      provides: "List of schedules with status badges, actions (pause/resume/delete/run-now)"
      min_lines: 80
    - path: "minipilot/app/src/lib/api.js"
      provides: "Schedule API client methods"
      contains: "getSchedules"
    - path: "minipilot/app/src/components/Sidebar.jsx"
      provides: "Schedule nav item with Clock icon"
      contains: "schedules"
    - path: "minipilot/app/src/components/WorkspaceShell.jsx"
      provides: "Routing for schedule and schedules pages"
      contains: "SchedulePage"
  key_links:
    - from: "minipilot/app/src/components/SchedulePage.jsx"
      to: "minipilot/app/src/lib/api.js"
      via: "api.createSchedule()"
      pattern: "api\\.createSchedule"
    - from: "minipilot/app/src/components/ScheduleListPage.jsx"
      to: "minipilot/app/src/lib/api.js"
      via: "api.getSchedules(), api.updateSchedule(), api.deleteSchedule()"
      pattern: "api\\.getSchedules"
    - from: "minipilot/app/src/components/WorkspaceShell.jsx"
      to: "minipilot/app/src/components/SchedulePage.jsx"
      via: "conditional render on currentPage === 'schedule'"
      pattern: "currentPage.*schedule"
---

<objective>
Frontend scheduling UI: API client methods, 4-step scheduling wizard, schedule list page with actions, sidebar navigation, and routing.

Purpose: Give users a visual interface to create, view, and manage recurring report schedules, completing the v1.1 Report Studio milestone.
Output: SchedulePage.jsx (wizard), ScheduleListPage.jsx (list/manage), updated api.js, Sidebar.jsx, WorkspaceShell.jsx
</objective>

<execution_context>
@/Users/paulduchateau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulduchateau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scheduling/04-RESEARCH.md
@.planning/phases/04-scheduling/04-01-SUMMARY.md

@minipilot/app/src/lib/api.js
@minipilot/app/src/components/WorkspaceShell.jsx
@minipilot/app/src/components/Sidebar.jsx
@minipilot/app/src/components/ImportReportPage.jsx (wizard pattern reference)
@minipilot/app/src/components/DashboardPage.jsx (layout pattern reference)

<interfaces>
<!-- API endpoints created in Plan 04-01 -->
GET    /api/w/:slug/schedules              — { schedules: Schedule[] }
POST   /api/w/:slug/schedules              — body: { name, frequency, hour, minute, day_of_week?, day_of_month?, end_date?, source_file_id?, template_report_id?, suggestion? } → { schedule }
GET    /api/w/:slug/schedules/:id          — { schedule, runs: ScheduleRun[] }
PATCH  /api/w/:slug/schedules/:id          — body: { status? } → { schedule }
DELETE /api/w/:slug/schedules/:id          — { ok: true }
GET    /api/w/:slug/schedules/:id/runs     — { runs: ScheduleRun[] }
POST   /api/w/:slug/schedules/:id/run-now  — { ok: true }
GET    /api/w/:slug/files                  — { files: UploadedFile[] }

<!-- Schedule object shape -->
```typescript
interface Schedule {
  id: string;
  workspace_id: string;
  name: string;
  frequency: 'once' | 'daily' | 'weekly' | 'monthly';
  cron_expression: string;
  hour: number;
  minute: number;
  day_of_week: number | null;
  day_of_month: number | null;
  end_date: string | null;
  source_file_id: string | null;
  template_report_id: string | null;
  suggestion: string | null;
  status: 'active' | 'paused' | 'completed' | 'error';
  edition_count: number;
  last_run_at: string | null;
  created_at: string;
}
```

<!-- Existing API client pattern from api.js -->
```javascript
export function createWorkspaceApi(slug) {
  const BASE = `${API_BASE}/w/${slug}`;
  return {
    methodName: async (args) => {
      const res = await fetch(`${BASE}/endpoint`, { method, headers, body });
      return res.json();
    },
    // ...
  };
}
```

<!-- Sidebar NAV_ITEMS pattern -->
```javascript
const NAV_ITEMS = [
  { id: "dashboard", Icon: LayoutDashboard, label: "Tableau de bord" },
  { id: "editor/new", Icon: FilePenLine, label: "Editeur" },
  { id: "chat", Icon: MessageSquare, label: "Explorer" },
  { id: "admin", Icon: Settings, label: "Admin" },
];
```

<!-- WorkspaceShell routing pattern -->
```jsx
{currentPage === "import" && (
  <ImportReportPage api={api} slug={slug} />
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API client methods and create SchedulePage wizard</name>
  <files>minipilot/app/src/lib/api.js, minipilot/app/src/components/SchedulePage.jsx</files>
  <read_first>
    - minipilot/app/src/lib/api.js (full file — add methods to createWorkspaceApi return object)
    - minipilot/app/src/components/ImportReportPage.jsx (full file — wizard UI pattern to follow)
    - .planning/phases/04-scheduling/04-RESEARCH.md (Pattern 4: Frontend Scheduling Wizard, DAYS_FR mapping)
    - minipilot/app/src/components/DashboardPage.jsx lines 1-50 (styling patterns)
  </read_first>
  <action>
    1. Add to `createWorkspaceApi()` return object in api.js — 8 new methods:
       - `getSchedules: async () => fetch(BASE + '/schedules').then(r => r.json())`
       - `createSchedule: async (data) => fetch(BASE + '/schedules', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify(data) }).then(r => r.json())`
       - `getSchedule: async (id) => fetch(BASE + '/schedules/' + id).then(r => r.json())`
       - `updateSchedule: async (id, data) => fetch(BASE + '/schedules/' + id, { method: 'PATCH', headers: {'Content-Type': 'application/json'}, body: JSON.stringify(data) }).then(r => r.json())`
       - `deleteSchedule: async (id) => fetch(BASE + '/schedules/' + id, { method: 'DELETE' }).then(r => r.json())`
       - `getScheduleRuns: async (id) => fetch(BASE + '/schedules/' + id + '/runs').then(r => r.json())`
       - `runScheduleNow: async (id) => fetch(BASE + '/schedules/' + id + '/run-now', { method: 'POST' }).then(r => r.json())`
       - `getFiles: async () => fetch(BASE + '/files').then(r => r.json())`

    2. Create `SchedulePage.jsx` — a 4-step wizard component following the ImportReportPage integer-step pattern:
       - Props: `{ api, slug }` (same pattern as ImportReportPage)
       - State: `step` (1-4 integer), `name`, `frequency` ('daily'), `hour` (8), `minute` (0), `dayOfWeek` (1), `dayOfMonth` (1), `endDate` (''), `sourceFileId` (null), `templateReportId` (null), `suggestion` (''), `useExisting` (true), `reports` ([]), `files` ([]), `creating` (false), `error` (null)
       - On mount: fetch reports via `api.getReports()` and files via `api.getFiles()`

       **Step 1 — Choisir le modele** (Choose template):
       - Radio group: "Rapport existant" vs "Nouveau rapport"
       - If existing: dropdown select populated from reports (show title)
       - If new: textarea for description (stored as `suggestion`)
       - Text input for schedule name (required)
       - "Suivant" button (disabled if no name or no template/description selected)

       **Step 2 — Configurer la frequence** (Configure schedule):
       - Frequency selector: 4 buttons (Une fois / Quotidien / Hebdomadaire / Mensuel) styled as pill radio buttons
       - Time picker: two `<select>` dropdowns for hour (0-23) and minute (0, 15, 30, 45)
       - Conditional fields:
         - If weekly: day of week select (Lundi-Dimanche, using DAYS_FR from research)
         - If monthly: day of month select (1-28, cap at 28 to avoid month-length issues)
       - End date: optional `<input type="date">` with label "Date de fin (optionnelle)"
       - "Suivant" / "Precedent" buttons

       **Step 3 — Source de donnees** (Data source):
       - Radio: "Utiliser les donnees actuelles" vs "Choisir un fichier source"
       - If file: dropdown from workspace files (show name + size)
       - Explanatory text: "Les donnees seront rafraichies avant chaque generation."
       - "Suivant" / "Precedent" buttons

       **Step 4 — Confirmation** (Confirm + Create):
       - Summary card showing: name, frequency description (use describeSchedule from research), template name or "Nouveau: [description excerpt]", data source name or "Donnees actuelles"
       - "Creer la programmation" primary button (disabled while creating)
       - On create: call `api.createSchedule({ name, frequency, hour, minute, day_of_week: dayOfWeek, day_of_month: dayOfMonth, end_date: endDate || null, source_file_id: sourceFileId, template_report_id: useExisting ? templateReportId : null, suggestion: useExisting ? null : JSON.stringify({ title: name, description: suggestion, type: 'bar', columns: [], kpis: [] }) })`
       - On success: navigate to `/${slug}/schedules`

       **Styling:** Follow existing app patterns:
       - Container: `padding: 32, maxWidth: 900, margin: '0 auto'`
       - Heading: Source Serif 4, fontSize 24, fontWeight 300
       - Step indicator: IBM Plex Mono, 11px, uppercase, showing "ETAPE {n}/4"
       - Buttons: DM Sans, borderRadius 10px, primary uses `var(--mp-accent)` bg
       - Cards/sections: `background: var(--mp-surface)`, border: `1px solid var(--mp-border)`, borderRadius 10px
       - Back button: ChevronLeft icon + "Retour" text, same pattern as ImportReportPage
  </action>
  <verify>
    <automated>cd /Users/paulduchateau/projects/litechange/minipilot/app && node -e "
      import fs from 'fs';
      const api = fs.readFileSync('src/lib/api.js', 'utf8');
      const methods = ['getSchedules','createSchedule','getSchedule','updateSchedule','deleteSchedule','getScheduleRuns','runScheduleNow','getFiles'];
      const missing = methods.filter(m => !api.includes(m));
      if (missing.length) { console.error('Missing methods:', missing); process.exit(1); }
      if (!fs.existsSync('src/components/SchedulePage.jsx')) { console.error('SchedulePage.jsx missing'); process.exit(1); }
      const page = fs.readFileSync('src/components/SchedulePage.jsx', 'utf8');
      if (!page.includes('step') || !page.includes('frequency')) { console.error('SchedulePage missing wizard logic'); process.exit(1); }
      console.log('OK');
    "
    </automated>
  </verify>
  <acceptance_criteria>
    - api.js contains 8 new methods: getSchedules, createSchedule, getSchedule, updateSchedule, deleteSchedule, getScheduleRuns, runScheduleNow, getFiles
    - SchedulePage.jsx exists with 4-step wizard (step state 1-4)
    - Step 1 has radio for existing/new report + name input
    - Step 2 has frequency selector (once/daily/weekly/monthly) + time + conditional day fields + end date
    - Step 3 has data source selector (current data vs file picker)
    - Step 4 has summary card + create button calling api.createSchedule
    - All text is in French
    - Uses var(--mp-*) CSS variables and var(--font-*) font families
  </acceptance_criteria>
  <done>SchedulePage wizard renders all 4 steps, api.js has all schedule methods</done>
</task>

<task type="auto">
  <name>Task 2: Create ScheduleListPage, wire routing and sidebar</name>
  <files>minipilot/app/src/components/ScheduleListPage.jsx, minipilot/app/src/components/WorkspaceShell.jsx, minipilot/app/src/components/Sidebar.jsx</files>
  <read_first>
    - minipilot/app/src/components/WorkspaceShell.jsx (full file — routing pattern)
    - minipilot/app/src/components/Sidebar.jsx lines 1-15 (NAV_ITEMS array + imports)
    - minipilot/app/src/components/DashboardPage.jsx lines 1-80 (list + card styling patterns)
    - minipilot/app/src/components/SchedulePage.jsx (just created — verify props)
  </read_first>
  <action>
    1. Create `ScheduleListPage.jsx` — displays all schedules for the workspace:
       - Props: `{ api, slug }` (same as other pages)
       - On mount: fetch schedules via `api.getSchedules()`
       - Header: "Programmations" (Source Serif 4, fontSize 24, fontWeight 300) + "Nouvelle programmation" button that navigates to `/${slug}/schedule`
       - If no schedules: empty state with Clock icon + "Aucune programmation" text + CTA button
       - Schedule cards in a vertical list, each showing:
         - Name (DM Sans, 16px, semibold)
         - Frequency description (use describeSchedule helper — inline in component, from research Pattern 4)
         - Status badge (IBM Plex Mono, 10px, uppercase, pill shape):
           - active: green dot + "ACTIF"
           - paused: yellow dot + "EN PAUSE"
           - completed: blue dot + "TERMINE"
           - error: red dot + "ERREUR"
         - Edition count: "N editions" (IBM Plex Mono, 12px)
         - Last run: relative date or "Jamais" if null
         - Action buttons row:
           - Pause/Resume toggle (Pause/Play icon)
           - Run now (Play icon with tooltip "Executer maintenant")
           - Delete (Trash2 icon, with confirmation window.confirm)
         - On pause: `api.updateSchedule(id, { status: 'paused' })`, refresh list
         - On resume: `api.updateSchedule(id, { status: 'active' })`, refresh list
         - On delete: `api.deleteSchedule(id)`, refresh list
         - On run-now: `api.runScheduleNow(id)`, show brief "Execution lancee" feedback, refresh list after 2s delay
       - Card styling: `background: var(--mp-surface)`, `border: 1px solid var(--mp-border)`, `borderRadius: 10px`, `padding: 20px`, `marginBottom: 12px`

    2. Update `Sidebar.jsx`:
       - Add Clock import from lucide-react: `import { ..., Clock } from "lucide-react"`
       - Add nav item to NAV_ITEMS array BEFORE "admin": `{ id: "schedules", Icon: Clock, label: "Programmations" }`

    3. Update `WorkspaceShell.jsx`:
       - Add imports at top:
         ```javascript
         import SchedulePage from "./SchedulePage";
         import ScheduleListPage from "./ScheduleListPage";
         ```
       - Add routing blocks in the content area (after the "import" block, before "chat"):
         ```jsx
         {currentPage === "schedule" && (
           <SchedulePage api={api} slug={slug} />
         )}
         {currentPage === "schedules" && (
           <ScheduleListPage api={api} slug={slug} />
         )}
         ```
       - Add `goToSchedule` helper: `const goToSchedule = () => navigate('/${slug}/schedule');`
       - Ensure the sidebar "schedules" nav item routes to the list page, and the wizard is accessed via the "schedule" (singular) route

    4. DAYS_FR constant for describeSchedule in ScheduleListPage:
       `const DAYS_FR = ['Dimanche','Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi'];`
  </action>
  <verify>
    <automated>cd /Users/paulduchateau/projects/litechange/minipilot/app && node -e "
      import fs from 'fs';
      if (!fs.existsSync('src/components/ScheduleListPage.jsx')) { console.error('Missing ScheduleListPage.jsx'); process.exit(1); }
      const sidebar = fs.readFileSync('src/components/Sidebar.jsx', 'utf8');
      if (!sidebar.includes('schedules') || !sidebar.includes('Clock')) { console.error('Sidebar missing schedules nav'); process.exit(1); }
      const shell = fs.readFileSync('src/components/WorkspaceShell.jsx', 'utf8');
      if (!shell.includes('SchedulePage') || !shell.includes('ScheduleListPage')) { console.error('WorkspaceShell missing schedule routing'); process.exit(1); }
      console.log('OK');
    "
    </automated>
  </verify>
  <acceptance_criteria>
    - ScheduleListPage.jsx exists, fetches and displays schedules with status badges
    - Each schedule card has pause/resume, run-now, and delete action buttons
    - Status badges use IBM Plex Mono uppercase with colored dots (active=green, paused=yellow, completed=blue, error=red)
    - Sidebar.jsx has "Programmations" nav item with Clock icon, positioned before "Admin"
    - WorkspaceShell.jsx routes "schedule" to SchedulePage and "schedules" to ScheduleListPage
    - Both new components imported in WorkspaceShell.jsx
    - Empty state shown when no schedules exist
    - All text is in French
  </acceptance_criteria>
  <done>Sidebar shows "Programmations" link, clicking it shows schedule list, "Nouvelle programmation" button leads to wizard, all schedule management actions work</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full scheduling flow</name>
  <files>none — verification only</files>
  <action>
    Human verification of the complete scheduling flow. No code changes — user tests the integrated system.
    Steps to verify:
    1. Start the server: cd minipilot/server and node index.js — confirm "Scheduler: N schedule(s) rehydrated" in console
    2. Start the app: cd minipilot/app and npm run dev
    3. Navigate to a workspace, click "Programmations" in sidebar
    4. Click "Nouvelle programmation" — verify 4-step wizard appears
    5. Step 1: Enter a name, select an existing report (or type a new description)
    6. Step 2: Select "Quotidien" frequency, set time to current hour+1, leave end date empty
    7. Step 3: Select "Utiliser les donnees actuelles"
    8. Step 4: Verify summary card shows correct info, click "Creer"
    9. Verify redirect to schedule list showing the new schedule with "ACTIF" badge
    10. Click "Executer maintenant" on the schedule — verify edition count increments after a moment
    11. Click "Pause" — verify badge changes to "EN PAUSE"
    12. Click "Reprendre" — verify badge changes back to "ACTIF"
    13. Restart the server — verify schedule rehydrated (check console log)
    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — requires manual testing"</automated>
  </verify>
  <done>User confirms full scheduling flow works: wizard creates schedule, list shows it, actions (run-now, pause, resume) work, server restart rehydrates</done>
</task>

</tasks>

<verification>
1. Server starts with scheduler rehydration log message
2. All 7 schedule API endpoints respond (test with curl or browser)
3. Sidebar shows "Programmations" nav item
4. Schedule wizard completes all 4 steps and creates a schedule
5. Schedule list shows schedules with correct status and edition count
6. Manual trigger creates a new report edition
7. Pause/resume toggles work and affect cron task registration
</verification>

<success_criteria>
- User can navigate to scheduling from sidebar
- User can create a schedule via the 4-step wizard (SCHED-01, SCHED-02, SCHED-05)
- User can select a data source file (SCHED-03)
- Manual trigger creates a numbered edition (SCHED-04)
- User can pause, resume, delete schedules from the list view
- All UI text is in French, follows LiteChange design system
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling/04-02-SUMMARY.md`
</output>
