---
phase: 03-import-de-rapports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/index.js
  - app/src/lib/api.js
autonomous: true
requirements:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-04

must_haves:
  truths:
    - "Server accepts .xlsx file upload on import-template endpoint and returns fingerprint JSON"
    - "Server accepts .docx file upload on import-template endpoint and returns fingerprint JSON"
    - "Fingerprint contains kpis array with label and unit fields"
    - "Fingerprint contains sections array with title, type, and fields"
    - "Fingerprint contains detectedFields array listing all detected columns/variables"
    - "API client exposes importTemplate method returning { fingerprint, availableColumns }"
  artifacts:
    - path: "server/index.js"
      provides: "POST /api/w/:slug/reports/import-template endpoint, uploadTemplate multer instance, parseDocxTemplate, parseXlsxTemplate, buildFingerprintPrompt"
      contains: "import-template"
    - path: "app/src/lib/api.js"
      provides: "importTemplate workspace API method"
      contains: "importTemplate"
  key_links:
    - from: "server/index.js"
      to: "mammoth"
      via: "import mammoth from mammoth"
      pattern: "import mammoth"
    - from: "server/index.js"
      to: "aiComplete"
      via: "AI fingerprint extraction"
      pattern: "aiComplete.*buildFingerprintPrompt"
    - from: "app/src/lib/api.js"
      to: "server/index.js"
      via: "fetch POST /api/w/:slug/reports/import-template"
      pattern: "import-template"
---

<objective>
Backend pipeline for template import: file upload (xlsx/docx), parsing, AI fingerprint extraction, and frontend API client.

Purpose: Enable the server to accept Excel and Word files as report templates, parse their content, extract structure via AI, and return a fingerprint that the frontend wizard (Plan 02) will consume.

Output: Working import-template endpoint + API client method.
</objective>

<execution_context>
@/Users/paulduchateau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulduchateau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-import-de-rapports/03-RESEARCH.md

<interfaces>
<!-- From app/src/lib/api.js — createWorkspaceApi factory pattern -->
export function createWorkspaceApi(slug) {
  const BASE = `/api/w/${slug}`;
  return {
    uploadFiles: async (files) => { ... },
    generateReport: async (suggestion) => { ... },
    // ... other methods
  };
}

<!-- From server/index.js — existing patterns -->
// multer storage (line 350): multer.diskStorage with UPLOADS_DIR + UUID prefix
// upload instance (line 358): multer({ storage, limits: 50MB, fileFilter for xlsx/csv/json })
// aiComplete (line 272): async function aiComplete(systemPrompt, userPrompt, { maxTokens })
// generate-report (line 2388): POST /api/w/:slug/ai/generate-report — parses suggestion, builds prompt, calls aiComplete, strips markdown fences, JSON.parse
// JSON cleanup pattern (line 2465): rawText.replace(/^```(?:json)?\n?/, "").replace(/\n?```$/, "")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install mammoth and create server-side parsing + AI fingerprint endpoint</name>
  <files>server/index.js, server/package.json</files>
  <read_first>
    - server/index.js (lines 1-30 for imports)
    - server/index.js (lines 350-376 for existing multer setup)
    - server/index.js (lines 270-310 for aiComplete)
    - server/index.js (lines 2388-2482 for generate-report pattern)
    - server/index.js (lines 575-600 for buildDataSummary pattern)
    - server/package.json (for current deps)
  </read_first>
  <action>
1. Install mammoth in server directory:
   `cd /Users/paulduchateau/projects/litechange/minipilot/server && npm install mammoth`

2. Add import at top of server/index.js (alongside existing imports):
   `import mammoth from "mammoth";`
   NOTE: Use default import only — mammoth is CJS; named imports from CJS are unreliable in ESM.

3. Create `uploadTemplate` multer instance right after the existing `upload` instance (after line 376). Reuse the same `storage`. Set fileSize limit to 20MB. fileFilter must accept BOTH mimetype AND extension for .xlsx/.xls/.docx/.doc (browsers sometimes send application/octet-stream for .docx):
   ```
   const uploadTemplate = multer({
     storage,
     limits: { fileSize: 20 * 1024 * 1024 },
     fileFilter: (req, file, cb) => {
       const ext = path.extname(file.originalname).toLowerCase();
       const allowedMimes = [
         "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
         "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
         "application/msword",
         "application/vnd.ms-excel",
       ];
       if (allowedMimes.includes(file.mimetype) || [".xlsx", ".xls", ".docx", ".doc"].includes(ext)) {
         cb(null, true);
       } else {
         cb(new Error("Seuls les fichiers .xlsx et .docx sont acceptés comme modèles."));
       }
     },
   });
   ```

4. Add helper functions in the Helpers section (after line 378):

   `parseDocxTemplate(filePath)` — reads file as buffer, calls `mammoth.convertToHtml({ buffer })`, truncates HTML to 8000 chars (3000 in local AI mode), returns truncated HTML string. Log warnings from mammoth if any.

   `parseXlsxTemplate(filePath)` — reads file as buffer, `XLSX.read(buffer, { type: "buffer" })`, maps SheetNames to `{ name, headers, sampleRows }` where headers = first row filtered for truthy values (skip null/undefined from merged cells), sampleRows = rows 1-3. Filter out sheets with 0 headers. Return array of sheet objects.

   `buildFingerprintPrompt(templateContent)` — returns the prompt string from RESEARCH.md Pattern 1 Phase B. Instructs AI to respond ONLY in valid JSON without markdown. Preserves original field label names (do not translate). Output shape: `{ title, objective, kpis: [{ label, unit }], sections: [{ title, type, fields }], detectedFields: [] }`.

5. Add the endpoint `POST /api/w/:slug/reports/import-template` using `uploadTemplate.single("file")`. Place it in a new section `// ── TEMPLATE IMPORT ──` before the chat-sessions block (around line 2484):
   - Return 400 if no file
   - Detect extension: .docx/.doc calls parseDocxTemplate, .xlsx/.xls calls parseXlsxTemplate (JSON.stringify the sheets array)
   - Call `aiComplete("", buildFingerprintPrompt(templateContent), { maxTokens: 2000 })`
   - Strip markdown fences from response (same pattern as generate-report line 2465)
   - JSON.parse the result into `fingerprint`
   - Query workspace columns: `SELECT columns FROM uploaded_files WHERE workspace_id = ?`, flatMap + dedupe into `allColumns`
   - Return `{ fingerprint, availableColumns: allColumns }`
   - Wrap in try/catch, log errors with `[POST import-template]` prefix, return 500 with error message
  </action>
  <verify>
    <automated>cd /Users/paulduchateau/projects/litechange/minipilot/server && node -e "import mammoth from 'mammoth'; console.log('mammoth OK:', typeof mammoth.convertToHtml)" && grep -c "import-template" index.js && grep -c "uploadTemplate" index.js && grep -c "parseDocxTemplate" index.js && grep -c "parseXlsxTemplate" index.js && grep -c "buildFingerprintPrompt" index.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "import mammoth" server/index.js returns 1 match
    - grep "uploadTemplate" server/index.js returns at least 2 matches (declaration + usage)
    - grep "parseDocxTemplate" server/index.js returns at least 2 matches (declaration + call)
    - grep "parseXlsxTemplate" server/index.js returns at least 2 matches (declaration + call)
    - grep "buildFingerprintPrompt" server/index.js returns at least 2 matches (declaration + call)
    - grep "import-template" server/index.js returns at least 1 match
    - mammoth is in server/package.json dependencies
    - Server starts without errors: cd server && timeout 5 node index.js 2>&1 || true (no SyntaxError/ImportError)
  </acceptance_criteria>
  <done>POST /api/w/:slug/reports/import-template endpoint exists, accepts .xlsx and .docx via uploadTemplate multer, parses file content, calls AI for fingerprint extraction, returns { fingerprint, availableColumns } JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Add importTemplate method to frontend API client</name>
  <files>app/src/lib/api.js</files>
  <read_first>
    - app/src/lib/api.js (full file — already read, 261 lines)
  </read_first>
  <action>
Add `importTemplate` method to the object returned by `createWorkspaceApi(slug)`, placed after `restoreReport` (around line 215). The method:

```javascript
importTemplate: async (file) => {
  const formData = new FormData();
  formData.append("file", file);
  const res = await fetch(`${BASE}/reports/import-template`, { method: "POST", body: formData });
  if (!res.ok) {
    const err = await res.json().catch(() => ({ error: "Erreur lors de l'import" }));
    throw new Error(err.error || "Erreur lors de l'import");
  }
  return res.json();
},
```

Key points:
- Uses FormData (same pattern as uploadFiles method at line 49)
- Throws on non-OK response (wizard needs to catch and display error)
- Returns `{ fingerprint, availableColumns }` from the server
  </action>
  <verify>
    <automated>grep -n "importTemplate" /Users/paulduchateau/projects/litechange/minipilot/app/src/lib/api.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "importTemplate" app/src/lib/api.js returns at least 1 match
    - grep "import-template" app/src/lib/api.js returns at least 1 match
    - grep "FormData" app/src/lib/api.js returns at least 2 matches (uploadFiles + importTemplate)
  </acceptance_criteria>
  <done>createWorkspaceApi returns an object with importTemplate(file) method that POSTs a file to /reports/import-template and returns { fingerprint, availableColumns }.</done>
</task>

</tasks>

<verification>
1. mammoth installed: `ls server/node_modules/mammoth/package.json`
2. Server boots without import errors: `cd server && timeout 5 node index.js 2>&1 | head -5`
3. Endpoint registered: `grep "import-template" server/index.js`
4. API client method exists: `grep "importTemplate" app/src/lib/api.js`
5. All helper functions present: `grep -E "parseDocxTemplate|parseXlsxTemplate|buildFingerprintPrompt|uploadTemplate" server/index.js | wc -l` (should be >= 8)
</verification>

<success_criteria>
- Server accepts .xlsx uploads on import-template endpoint (IMP-01)
- Server accepts .docx uploads on import-template endpoint (IMP-02)
- AI fingerprint extraction returns kpis array (IMP-03)
- AI fingerprint extraction returns sections with type/fields (IMP-04)
- Frontend API client has importTemplate method ready for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/03-import-de-rapports/03-01-SUMMARY.md`
</output>
